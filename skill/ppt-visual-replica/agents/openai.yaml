interface:
  display_name: "PPT Visual Replica"
  short_description: "Editable replicas and evidence-based scientific slides"
  default_prompt: "Use $ppt-visual-replica to create an editable presentation from these sources: generate each slide draft with $imagegen first, reconstruct it as editable PPTX, then verify the rendered result."
