interface:
  display_name: "PPT Visual Replica"
  short_description: "Faithful editable PPT replicas with strict asset validation"
  default_prompt: "Use $ppt-visual-replica to recreate this reference as an editable PPTX and deliver it only after residual, asset, metadata, and rendered-preview validation pass."
